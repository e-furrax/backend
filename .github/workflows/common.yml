name: Common Jobs CI

on:
    push:
    pull_request:

jobs:
    lint:
        name: 'Linter check'
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1
            - uses: stefanoeb/eslint-action@1.0.2
              with:
                  files: src/
